🔧 Swift 코드 오류 수정 가이드

## 발견된 오류들:

### 1. User.swift - Regex 오류:
```swift
// 잘못된 코드:
let emailRegex = /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i

// 올바른 코드:
let emailRegex = "^[A-Z0-9._%+-]+@[A-Z0-9.-]+\\.[A-Z]{2,}$"
```

### 2. User.swift - 최상위 표현식 오류:
```swift
// import 문 다음에 바로 코드 작성하지 말고, struct/class 안에 넣기
```

### 3. ChatManager.swift - 클로저 타입 어노테이션:
```swift
// 잘못된 코드:
.sink { _ in }
.sink { counts in }

// 올바른 코드:
.sink { (completion: Subscribers.Completion<Error>) in }
.sink { (counts: MessageCounts) in }
```

## 수정할 파일들:
1. User.swift
2. ChatManager.swift

이 오류들을 수정하면 앱이 정상적으로 빌드됩니다.